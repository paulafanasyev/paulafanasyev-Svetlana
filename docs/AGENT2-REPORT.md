# Agent 2 handoff — Svetlana migration

## Decision

The project direction is **Aster-first**. The working Aster Android agent is the technical base for Svetlana. MobileAgent and Sanna are deferred and are NOT runtime dependencies of the first Svetlana build. OX2/Hands remains frozen and separate.

## Product rule

We are not replacing Aster's working voice stack. **Existing Aster voice is frozen and preserved unchanged.** Accessibility, execution, MCP/Binder, tools and security are also preserved until runtime evidence proves a reason to change them.

## Changes made

1. `README.md` switched to Aster-first.
2. Added `.github/workflows/import-aster.yml` to clone the complete upstream Aster tree into this repository, preserve the Svetlana docs, and set the Android application label to `Светлана` without rewriting internal Aster identifiers during the first import.
3. Added the migration contract generated by the import job as `docs/ASTER-IMPORT.md`.
4. Added and merged PR #1 (`edbc1162a40d2f70a844b616624fb0e66817b90f`) solely to trigger the import workflow through GitHub Actions.

## Current execution state

The import mechanism is committed and the trigger PR is merged. However, a direct repository check still does **not** show `apps/android/app/src/main/AndroidManifest.xml`, so the Aster source import is **NOT PROVEN COMPLETE** yet. I am explicitly not calling this a successful import until the source tree appears in the target repository.

## Not claimed

- No APK build PASS.
- No runtime PASS.
- No claim that Russian localization is complete.
- No package-identifier rename yet; internal Aster identifiers remain preserved for the first stable import.
- No OX2/Hands changes.
- Azure has not been restored.

## Next evidence gate

1. Confirm the GitHub Actions import actually wrote the Aster source tree.
2. Inspect the imported tree and build the Android app.
3. Verify the existing Aster voice stack and Accessibility control.
4. Only after that, finish Russian user-facing localization/persona without destabilizing voice/execution.
